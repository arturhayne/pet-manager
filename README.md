# Template completo com Webpack

## Instalação

``` bash
# Instalar as dependências
yarn

# Iniciar o servidor at localhost:4200
yarn dev
```

## Dicas

- axios.defaults.headers.common.Authorization = `Bearer ${token}`;
